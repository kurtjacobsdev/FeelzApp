import XCTest
@testable import SettingsFeatureDomain

final class SettingsFeatureDomainTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SettingsFeatureDomain().text, "Hello, World!")
    }
}
